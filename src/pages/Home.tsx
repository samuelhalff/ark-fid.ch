import Hero from "@/components/ui/hero";
import React from "react";

const Home: React.FC = () => {
  return (
    <div style={{ padding: "2rem", textAlign: "center" }}>
      <Hero />
    </div>
  );
};

export default Home;
